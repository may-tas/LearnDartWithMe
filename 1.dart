
void main(){
/* Variables int , String, double , bool , dyanamic ( can take all values but not preferred as has very less properties.)
   Variables var/final/const 
   var is used widely as it correctly identifies datatype of whatever value u store in it and behave as that datatype.
  
   var greeting = '10';
   greeting= 4; //not allowed as u cant assign an integer value to string type var.
 
  // Both final and const values cant be reassigned as they are constants.

   final someValue1 = DateTime.now();   Runtime constant
   const somValue2 = DateTime.now();    compile time constant , Hence error 
   

*/
}
