
bool isAllowed = false;

void main(){

  // If statements
  // boolean condn are used in the brackets to get true or false for statements to get executed.
/*
   int age = 19;

   if(age>=21){
    print("Adult 21");
   }else if(age>=18){
    print("Adult");
   }
   else{       // else condition is option even without code will run fine.
    print("Child");
   }
*/
 // Similar relational and assignment operators as C++.
 // eg. != is not equal to and == is equal to , <= , >= , < , > 


///////////////////////////////////////////////

 // isAllowed=!isAllowed;
 // int age = 19;

 /* if(age>=18 && isAllowed==true){
    print("adult");
  }else{
    print(("child"));
  }
*/
 ///////////////////////////
 
 /*  int age = 19;
   if(age>=18 && !isAllowed){
    print("Adult");
   }else{print("child");} 

  // OR operator ||

   int age1 = 19;
   if(age1>=18 || isAllowed==true){  // can also just write if(age1>=18 || isAllowed)
           print("Adult");
   }
*/
//NOTE:
// curly brackets of if and else or any method defines their scope so if u define a variable in if scope then u can't access it outside the if statement elsewher.


}